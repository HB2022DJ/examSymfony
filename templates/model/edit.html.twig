<!DOCTYPE html>

<title>Editer le model</title>

{% block body %}
	<h1>Editer le Model</h1>

	{{ include('model/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('app_model_index') }}">retour</a>

	{{ include('model/_delete_form.html.twig') }}
{% endblock %}
