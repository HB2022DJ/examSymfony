<!DOCTYPE html>

<title>Editer la marque</title>

{% block body %}
	<h1>Editer la marque</h1>

	{{ include('brand/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('app_brand_index') }}">retour</a>

	{{ include('brand/_delete_form.html.twig') }}
{% endblock %}
